#include<stdio.h>
void main()
{
    int a;   // variable decleratio  >> a is name of variable whose data datatype in int(integer)means a can store the integer value only
    printf("the size of int is %ld byte\n",sizeof(int));   //
    a=4;     // initialization means integer 4 is store into the variable a  
    printf("the value of a is: %d",a);  // %d is identifier where decimal value print which store in variable a
}